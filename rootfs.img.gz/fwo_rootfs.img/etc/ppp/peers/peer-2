# /etc/ppp/peers/peer-2

# don't require peer to authenticate itself
noauth

# connect/disconnect chat scripts
connect "/usr/sbin/chat -f /etc/ppp/chat/connect-2"
disconnect "/usr/sbin/chat -f /etc/ppp/chat/disconnect-2"

