# /etc/ppp/peers/peer-3

# don't require peer to authenticate itself
noauth

# peer doesn't support compression control protocol
noccp

connect "/usr/sbin/chat -f /etc/ppp/chat/connect-3"
disconnect "/usr/sbin/chat -f /etc/ppp/chat/disconnect-3"

